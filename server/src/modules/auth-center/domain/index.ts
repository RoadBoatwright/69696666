/**
 * 账户授权中心领域类型导出（组件 2，需求 2-5）。
 */
export type {
  AuthPlatformId,
  MetaAuthParams,
  GoogleAuthParams,
  TikTokAuthParams,
  IssuedToken,
  RefreshedToken,
  AuthorizeSuccess,
  AuthorizeFailure,
  AuthorizeResult,
  AuthorizationSummaryRow,
} from './auth';
